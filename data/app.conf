# ------------------------------------
# Main Application Configuration
# ------------------------------------

# Application details
APP_NAME=Personal-Finance-App
VERSION=2.0

# Environment settings (can be 'development' or 'production')
ENVIRONMENT=development

# Enable or disable debug mode
DEBUG_MODE = true

# Path for application logs (Note the spaces around '=')
 LOG_FILE_PATH = /var/log/pfa/main.log